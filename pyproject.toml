[project]
name = "photo-sorter"
version = "1.0.1"
description = "A small utility that organizes photos into month-wise and date-wise folders."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=10.0.0",
    "pyinstaller>=6.0.0",
]
